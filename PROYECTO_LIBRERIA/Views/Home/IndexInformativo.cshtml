@{
    ViewData["Title"] = "Información del Sistema";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    .card {
        border-radius: 10px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        border: none;
    }

    .user-header {
        background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
        color: white;
        border-radius: 10px 10px 0 0;
        padding: 20px;
    }

    .system-header {
        background: linear-gradient(135deg, #ff9a9e 0%, #fad0c4 100%);
        color: #333;
        border-radius: 10px 10px 0 0;
        padding: 20px;
    }

    .module-badge {
        margin: 5px;
        font-size: 0.85rem;
    }

    .alert-warning {
        border-left: 4px solid #ffc107;
    }
</style>

<div class="container my-4">
    <div class="card mb-5">
        <div class="user-header">
            <h2 class="mb-0"><i class="bi bi-person-circle me-2"></i>Información del Usuario</h2>
        </div>
        <div class="card-body">
            <div class="row g-4">
                <div class="col-md-6 border-end">
                    <h5 class="text-primary border-bottom pb-2">Datos de Sesión</h5>
                    <div class="mb-3 mt-3">
                        <label class="form-label fw-bold text-muted small uppercase">Nombre Completo:</label>
                        <p class="fs-5 fw-semibold">@ViewBag.NombreCompleto</p>
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-bold text-muted small">ID de Usuario (Login):</label>
                        <p class="form-control-plaintext">@ViewBag.UsuarioLogin</p>
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-bold text-muted small">Rol Asignado:</label>
                        <p><span class="badge bg-primary fs-6">@ViewBag.Rol</span></p>
                    </div>
                </div>
                <div class="col-md-6">
                    <h5 class="text-primary border-bottom pb-2">Módulos Accesibles</h5>
                    <p class="text-muted small">Usted tiene acceso a los siguientes módulos según su rol:</p>
                    <div class="d-flex flex-wrap">
                        <span class="badge bg-success module-badge"><i class="bi bi-check2-circle me-1"></i>Generar Venta</span>
                        <span class="badge bg-success module-badge"><i class="bi bi-check2-circle me-1"></i>Generar Cotización</span>
                        <span class="badge bg-success module-badge"><i class="bi bi-check2-circle me-1"></i>Consultar Venta</span>
                        <span class="badge bg-success module-badge"><i class="bi bi-check2-circle me-1"></i>Reportes</span>
                        @if (User.IsInRole("ADMINISTRADOR"))
                        {
                            <span class="badge bg-dark module-badge"><i class="bi bi-shield-lock me-1"></i>Configuración Total</span>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="card">
        <div class="system-header">
            <h2 class="mb-0"><i class="bi bi-info-square me-2"></i>Información del Sistema</h2>
        </div>
        <div class="card-body">
            <div class="alert alert-warning d-flex align-items-center mt-3">
                <i class="bi bi-exclamation-triangle-fill fs-3 me-3"></i>
                <div>
                    Se recomienda cerrar sesión al finalizar su labor. El sistema detectará inactividad y le notificará para proteger sus datos.
                </div>
            </div>

            <div class="alert alert-info d-flex align-items-center">
                <i class="bi bi-shield-check fs-3 me-3"></i>
                <div>
                    Acceso restringido: El sistema utiliza control de roles (RBAC). Si no puede ver un módulo, contacte al administrador.
                </div>
            </div>
        </div>
    </div>
</div>